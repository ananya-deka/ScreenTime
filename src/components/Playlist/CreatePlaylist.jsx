import classes from "./CreatePlaylist.module.css";

const CreatePlaylist = () => {
	return (
		<button className={classes.create_button}>Create a new Playlist</button>
	);
};

export default CreatePlaylist;
